name: Arch live-stream-segmenter-git

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    container:
      image: archlinux:base-devel

    permissions:
      contents: read

    steps:
      - name: Checkout project repository
        uses: actions/checkout@v6

      - name: Setup builder
        shell: bash
        run: |
          useradd -m builder
          echo 'builder ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/builder
          chown -R builder:builder .
          pacman -Syu --noconfirm git

      - name: Install quickjs-ng
        shell: bash
        run: |
          git clone https://aur.archlinux.org/quickjs-ng.git
          chown -R builder:builder cpp-httplib
          cd quickjs-ng
          sudo -u builder makepkg -si --noconfirm

      - name: Build
        shell: bash
        working-directory: arch/live-stream-segmenter-git
        run: sudo -u builder makepkg -s --noconfirm
