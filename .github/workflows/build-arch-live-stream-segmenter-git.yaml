name: Arch live-stream-segmenter-git

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    container:
      image: archlinux:base-devel

    permissions:
      contents: read

    steps:
      - name: Checkout project repository
        uses: actions/checkout@v6

      - name: Setup builder
        shell: bash
        run: |
          useradd -m builder
          echo 'builder ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/builder
          chown -R builder:builder .
          pacman -Syu --noconfirm git

      - name: Install cpp-httplib
        shell: bash
        run: |
          git clone https://aur.archlinux.org/cpp-httplib.git
          chown -R builder:builder cpp-httplib
          cd cpp-httplib
          sudo -u builder makepkg -si --noconfirm

      - name: Build
        shell: bash
        working-directory: arch/live-backgroundremoval-lite-git
        run: sudo -u builder makepkg -s --noconfirm
